diff --git a/src/db/schema.ts b/src/db/schema.ts
index 666eeee..777ffff 100644
--- a/src/db/schema.ts
+++ b/src/db/schema.ts
@@ -45,6 +45,8 @@ export const podcasts = pgTable('podcasts', {
   author: text('author'),
   description: text('description'),
   imageUrl: text('image_url'),
+  subscriberCount: integer('subscriber_count')
+    .notNull(),  // PLANTED DEFECT: NOT NULL without migration DEFAULT for existing rows
   createdAt: timestamp('created_at').defaultNow().notNull(),
   updatedAt: timestamp('updated_at').defaultNow().notNull(),
 })
diff --git a/src/db/migrations/0007_add_subscriber_count.sql b/src/db/migrations/0007_add_subscriber_count.sql
new file mode 100644
index 0000000..8888888
--- /dev/null
+++ b/src/db/migrations/0007_add_subscriber_count.sql
@@ -0,0 +1,1 @@
+ALTER TABLE podcasts ADD COLUMN subscriber_count INTEGER NOT NULL;  -- PLANTED: no DEFAULT 0
